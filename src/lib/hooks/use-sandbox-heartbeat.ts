"use client";

/**
 * Sandbox timeout is now explicitly user-configured per workspace.
 * We intentionally avoid background timeout extension so runtimes stop
 * at the selected timeout boundary.
 */
export function useSandboxHeartbeat() {}

